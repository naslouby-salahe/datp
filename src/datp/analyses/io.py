from __future__ import annotations

from pathlib import Path

import numpy as np


def load_cal_errors(score_root: Path) -> np.ndarray:
    raise NotImplementedError("load_cal_errors is not available in this branch")
