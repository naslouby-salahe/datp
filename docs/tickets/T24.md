# T24 — CICIoT2023 B-b Training and Evaluation

## Status

`SKIPPED_WITH_REASON`

## Skip Reason

CICIoT2023 B-b training/evaluation skipped because the verified raw CSV schema contains no MAC, device ID, client ID, capture-source identifier, source/destination IP, or other usable client partition metadata. H02 is closed; the branch is rejected by feasibility audit, not blocked by missing user action.

CICIoT2023 B-b is infeasible on the currently available CSV artifact. No client partition is built from folders, merged files, labels, row chunks, or filenames; doing so would fabricate pseudo-clients and is prohibited.

## Phase

Phase 4 — Experiment execution (Gate 2 — Regime B-b)

## Source Plan References

- `docs/journal/CODING_PLAN.md` §6 (GD-04, GD-05, GD-06)
- `docs/journal/EXPERIMENT_PLAN.md` §6.2 (Regime B-b)

## Goal

Train shared AE per (Regime B-b, seed), score clients, apply B1/B2/B4 thresholds, and evaluate. Apply stored-score variants on B-b scores.

## Why This Exists

Regime B-b is conditional external CICIoT2023 validation with real device/MAC clients instead of file-level pseudo-clients.

## Scope

- Shared AE training per (Regime B-b, seed)
- Client scoring
- B1/B2/B4 threshold evaluation
- B3 only if valid non-circular group mapping
- Stored-score variants after verification
- Clear partition labeling (MAC-based or group-based)

## Out of Scope

- Modifying Regime B-a
- Adding new datasets

## Human Intervention Required

Status: `NONE` (H02 closed 2026-05-24; no further user action required for this ticket).

## Dependencies

- Previous ticket: T23 (B-b partition must exist)
- Required files: Processed B-b splits from T23
- Required decisions: T23 feasibility outcome
- Required artifacts: T23 preprocessed data

## Previous-Ticket Check

Before starting: confirm T23 is `DONE` with feasible outcome.

## Existing Code to Inspect First

- `src/datp/training/fl/runner.py`
- `src/datp/sweep/run_sweep.py`
- `src/datp/baselines/main/`

## Existing Tests to Inspect First

- `tests/e2e/regime_b/`
- `tests/integration/training/`

## Files Likely Touched

- Config (B-b regime config)
- `outputs/scores/b_b/`, `outputs/results/b_b/` (new artifacts)

## Implementation Requirements

1. Train FL encoder with B-b partition
2. Score all B-b clients
3. Apply B1/B2/B4 thresholds
4. B3 only if valid non-circular mapping exists
5. Apply stored-score variants
6. Report with correct partition labeling

## Refactoring Requirements

- Reuse existing FL training and evaluation infrastructure

## Schema, Enum, Constant, and Config Checks

- B-b regime labeling in outputs
- Partition type label (MAC vs group) in result metadata

## Test Requirements

- Integration tests after data exists

## Commands to Run

- Equivalent of `make run-regime-b-b` (to be created)

## Full-Suite Trigger Condition

Adds new regime partition — run integration tests after stabilization.

## Acceptance Criteria

1. Training converges
2. Baselines evaluated
3. Partition type correctly labeled
4. B3 suppressed if circular mapping

## Stop Conditions

- Ticket is `SKIPPED_WITH_REASON`. No training, scoring, or partitioning is to be performed.
- Do not re-open or re-classify this ticket based on folders, merged files, labels, row chunks, filenames, or any other pseudo-client/pseudo-time signal.

## Implementation Notes

B-b is rejected on the currently available CICIoT2023 CSV artifact (T23 outcome `B_B_REJECTED_NO_METADATA`). This ticket is skipped and the paper reports CICIoT2023 under Regime B-a only.
