# T15 — Regime C Severity Analysis

## Status

`NOT_STARTED`

## Phase

Phase 2 — Stored-score analyses (Group GB)

## Source Plan References

- `docs/journal/CODING_PLAN.md` §4 (GB-11)
- `docs/journal/CODING_PLAN.md` §3 (GA-09 — Regime C completeness)
- `docs/journal/EXPERIMENT_PLAN.md` §4 (Gate 1)

## Goal

Analyze CV(FPR) gap over α values in Regime C. Produce severity table and figure showing how heterogeneity severity (controlled by Dirichlet α) relates to B1−B2 gap.

## Why This Exists

Regime C provides supportive evidence that the threshold personalization effect scales with data heterogeneity severity.

## Scope

- Verify Regime C completeness: `α ∈ {0.1, 0.3, 0.5, 1.0, 10.0, IID}`, seeds 0–4
- For each verified α cell: extract B1 and B2 CV(FPR)
- Compute B1−B2 gap per α
- Produce severity table and trend figure
- Document any missing α/IID cells with explicit suppression notes

## Out of Scope

- Retraining Regime C
- Adding new α values
- Regime C seed extension (that is a separate decision)

## Human Intervention Required

Status: `NO`

## Dependencies

- Previous ticket: T04 (reuse verdict includes Regime C cells)
- Required files: `outputs/scores/c/seed_{0..4}/alpha_*/`, `outputs/results/c/`
- Required decisions: None
- Required artifacts: Cell verdict table (must include Regime C cells)

## Previous-Ticket Check

Before starting: confirm T01–T04 are `DONE` or correctly blocked.

## Existing Code to Inspect First

- `src/datp/data/regimes/regime_c.py` — Regime C logic
- `src/datp/config/compose.py` — how alpha flows
- Stored results: `outputs/results/c/`

## Existing Tests to Inspect First

- `tests/e2e/regime_c/` — Regime C e2e tests
- `tests/integration/data/regime_c/`

## Files Likely Touched

- `src/datp/` (severity analysis module)
- `tests/unit/` (new tests)
- `outputs/analysis/` (output artifacts)

## Implementation Requirements

1. Enumerate Regime C cells from stored results
2. Verify completeness against expected α grid
3. Extract B1/B2 CV(FPR) per (α, seed)
4. Compute mean B1−B2 gap per α across seeds
5. Produce severity table (α vs gap)
6. Produce trend figure
7. Missing cells: explicit suppression note in output

## Refactoring Requirements

- Reuse existing Regime C path utilities
- No new regime logic needed

## Schema, Enum, Constant, and Config Checks

- α values should come from config (not inline list)
- Use `Regime.C` enum consistently

## Test Requirements

- Unit test: correct gap computation for known inputs
- Unit test: missing α cell produces suppression note, not error

## Commands to Run

- `make test-unit` (targeted tests)

## Full-Suite Trigger Condition

Not breaking — no full suite needed.

## Acceptance Criteria

1. Severity table includes all verified α values
2. Missing cells documented with suppression notes
3. Figure shows trend (monotone or not — reported honestly)
4. No new α values added

## Stop Conditions

- Stop if Regime C cells are largely incomplete (document and report partial)

## Implementation Notes

Current repository status shows Regime C results exist for seeds 0–4 across α values. Completeness must be verified by T02/T04 before this analysis runs.
