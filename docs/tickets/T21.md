# T21 — Edge-IIoTset Dataset Spec and Preprocessing

## Status

`BLOCKED_HUMAN`

## Phase

Phase 2/4 — Conditional external validation (Group GC)

## Source Plan References

- `docs/journal/CODING_PLAN.md` §5 (GC-01, GC-02, GC-03)
- `docs/journal/PRE_CODING_PLAN.md` §5.5 (Edge-IIoTset Feasibility)
- `docs/journal/PRE_CODING_PLAN.md` §5.10 (Feature-Modality Audit)
- `docs/journal/EXPERIMENT_PLAN.md` §6.1 (Regime D)

## Goal

Add Edge-IIoTset dataset spec, preprocessing pipeline, and client partition after the dataset is downloaded and the Gate 0 feasibility audit passes.

## Why This Exists

Regime D (Edge-IIoTset) is conditional external validation. The dataset is not present in the repository. All Regime D work is blocked until the dataset is obtained and audited.

## Scope

- Download and place Edge-IIoTset raw files
- Feature-modality audit (§5.10): network-only vs full mixed features
- Temporal-validity audit (§5.11): timestamp availability
- Dataset spec: feature schema, feature count, client identifiers
- Preprocessing: deterministic Parquet train/cal/test splits
- Client partition: device or group clients per feasibility outcome

## Out of Scope

- Training (that is T22)
- Threshold evaluation (that is T22)
- Altering Regime A confirmatory claim

## Human Intervention Required

Status: `YES`

- Required action: Download Edge-IIoTset from Ferrag et al. (2022) IEEE Access source
- Required path: Place raw files in `data/raw/Edge-IIoTset/` (or path to be defined after dataset structure is known)
- Blocking reason: Dataset not present anywhere in repository (confirmed by Phase 0 GA-08 audit)
- Unblocks: T21 (this ticket), T22 (training/evaluation), T25 (temporal probe if feasible)

## Dependencies

- Previous ticket: T01 (data root resolved)
- Required files: Raw Edge-IIoTset files (NOT PRESENT)
- Required decisions: Feature modality (network-only vs mixed) — decided during audit
- Required artifacts: None

## Previous-Ticket Check

Before starting: confirm T01 is `DONE`. This ticket is BLOCKED_HUMAN regardless.

## Existing Code to Inspect First

- `src/datp/data/datasets/nbaiot/spec.py` — pattern for dataset spec
- `src/datp/data/datasets/ciciot2023/spec.py` — pattern for dataset spec
- `src/datp/data/datasets/nbaiot/prepare.py` — preprocessing pattern
- `src/datp/data/catalog.py` — dataset catalog

## Existing Tests to Inspect First

- `tests/integration/data/nbaiot/` — data integration test pattern
- `tests/unit/data/` — data unit test pattern

## Files Likely Touched

- `src/datp/data/datasets/edge_iiotset/` (new package: spec.py, prepare.py)
- `src/datp/data/catalog.py` (register new dataset)
- `src/datp/core/enums.py` (add Regime.D if not present)
- `tests/` (new tests)

## Implementation Requirements

1. After download: enumerate raw files, schema, column types
2. Feature-modality decision: network-only if cleanly separable
3. Create `spec.py`: FEATURE_COLUMNS, DEVICE_DIRS or CLIENT_DIRS, FEATURE_COUNT
4. Create `prepare.py`: deterministic preprocessing to Parquet
5. Client partition per feasibility outcome:
   - `EDGE_FEASIBLE_DEVICE_CLIENTS`: physical device clients, K ≥ 6
   - `EDGE_FEASIBLE_GROUP_CLIENTS_ONLY`: group clients only
6. Eligibility: n_cal ≥ 100 per client, ≥ 90% coverage

## Refactoring Requirements

- Follow exact pattern of existing dataset specs
- Reuse data preparation utilities from existing code

## Schema, Enum, Constant, and Config Checks

- May need `Regime.D` enum member
- May need `DatasetID.EDGE_IIOTSET` member
- Feature columns as tuple constant in spec

## Test Requirements

- Schema and feature count tests
- Determinism test for preprocessing
- Client partition eligibility tests
- Leakage tests (train/test separation)

## Commands to Run

- None until dataset is available

## Full-Suite Trigger Condition

Adds new dataset — run relevant integration tests after stabilization.

## Acceptance Criteria

1. Dataset downloaded and placed
2. Feature-modality audit complete
3. Preprocessing produces valid Parquet splits
4. Client partition produces ≥ 6 eligible clients (device) or ≥ 8 (group)
5. Feasibility outcome assigned: one of the allowed values

## Stop Conditions

- Stop (BLOCKED_HUMAN) until dataset is downloaded
- Stop if feasibility outcome is REJECTED — suppress Regime D
- Stop if metadata is insufficient for client identification

## Implementation Notes

Phase 0 GA-08 finding: `EDGE_BLOCKED_DATA_UNAVAILABLE`. This ticket cannot proceed without user action. All Regime D results depend on this ticket.
