# T22 — Edge-IIoTset Training and Evaluation

## Status

`NOT_STARTED`

## Phase

Phase 4 — Experiment execution (Gate 2 — Regime D)

## Source Plan References

- `docs/journal/CODING_PLAN.md` §5 (GC-04, GC-05, GC-06)
- `docs/journal/EXPERIMENT_PLAN.md` §6.1 (Regime D)

## Goal

Train one shared AE per (Regime D, seed), score clients, apply B1/B2/B4 thresholds, and evaluate. Apply stored-score variants (q-sensitivity, τ-shrink, B2-conf, B-FedStatsBenign) on Regime D scores after verification.

## Why This Exists

Regime D is external validation of the threshold-scope effect on a second IoT benchmark. Requires T21 to complete first.

## Scope

- Shared AE training per (Regime D, seed)
- Client scoring
- B1/B2/B4 threshold evaluation
- B3 only if valid non-circular group mapping exists
- Stored-score variants on Regime D scores after verification
- Metrics and table rows for Regime D

## Out of Scope

- Altering Regime A claim
- Making Regime D a primary confirmatory dataset

## Human Intervention Required

Status: `RESOLVED` (H01 closed 2026-05-24); now ticket-blocked by T21.

- Resolved action: Edge-IIoTset raw files are present at `data/raw/Edge-IIoTset/`.
- Remaining dependency: T21 must complete (preprocessed Parquet splits and feasibility outcome) before this ticket can begin.

## Dependencies

- Previous ticket: T21 (preprocessing must complete first)
- Required files: Processed Edge-IIoTset from T21
- Required decisions: T21 feasibility outcome determines scope
- Required artifacts: T21 preprocessed data

## Previous-Ticket Check

Before starting: confirm T21 is `DONE`.

## Existing Code to Inspect First

- `src/datp/training/fl/runner.py` — FL training
- `src/datp/sweep/run_sweep.py` — sweep execution
- `src/datp/baselines/main/` — baseline evaluation

## Existing Tests to Inspect First

- `tests/integration/training/test_fl_simulation.py`
- `tests/e2e/regime_a/` — pattern for regime evaluation

## Files Likely Touched

- `src/datp/data/regimes/` (add regime_d.py if needed)
- `src/datp/core/enums.py` (REGIME_BASELINES for D)
- Config files (Regime D config)
- `outputs/scores/d/`, `outputs/results/d/` (new artifacts)

## Implementation Requirements

1. Train FL encoder with Regime D data (same hyperparameters, input_dim adjusted)
2. Score all Regime D clients
3. Apply B1/B2/B4 thresholds
4. B3: suppress if group-partitioned or circular mapping
5. Apply GB analyses (q, τ-shrink, B2-conf, B-FedStatsBenign)
6. Report with explicit partition labeling (device vs group)

## Refactoring Requirements

- Reuse existing FL training infrastructure with input_dim parameter
- Reuse existing threshold/metric code

## Schema, Enum, Constant, and Config Checks

- Regime D in `REGIME_BASELINES` map
- B3 suppression logic for group-partitioned Regime D

## Test Requirements

- Integration test for Regime D training (after data exists)
- B3 suppression test for group-partitioned case

## Commands to Run

- Equivalent of `make run-regime-d` (to be created)

## Full-Suite Trigger Condition

Adds new regime — run integration tests after stabilization.

## Acceptance Criteria

1. Training converges for Regime D
2. All baseline metrics computed
3. B3 suppressed if appropriate
4. Results labeled with correct partition type
5. Stored-score variants applied

## Stop Conditions

- Blocked until T21 completes
- Stop if training fails to converge
- Stop if feasibility outcome was REJECTED in T21

## Implementation Notes

A separate AE is trained per dataset. The Edge-IIoTset AE uses the same hyperparameter specification as N-BaIoT but with `input_dim` matched to Edge-IIoTset's feature count.
