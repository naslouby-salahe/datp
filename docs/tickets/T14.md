# T14 — B3 Preservation

## Status

`DONE`

## Resolution Record (DONE on 2026-05-24)

- New module `src/datp/analyses/mechanism/b3_preservation.py`:
  - `run_b3_preservation(base_dir, *, config=None, write_outputs=False) → B3PreservationResult`
  - B3 via canonical `derive_threshold(Baseline.B3, ...)` using `DEVICE_FAMILY_MAP`
  - Compares against stored `metrics.json` when available; gracefully skips when absent
  - `B3Row` / `B3PreservationResult` pydantic schemas (extra='forbid', frozen=True)
- Writes `b3_preservation.csv`
- No retraining — reads stored scores only
- Tests: `tests/unit/analyses/mechanism/test_b3_preservation.py` (4 cases)
- ruff: 0 errors. pyright: 0 errors.

## Phase

Phase 2 — Stored-score analyses (Group GB)

## Source Plan References

- `docs/journal/CODING_PLAN.md` §4 (GB-10)
- `docs/journal/EXPERIMENT_PLAN.md` §4 (Gate 1)

## Goal

Reproduce Regime A B3 from stored scores and include in journal artifacts. Verify B3 reproduces within tolerance of stored reference.

## Why This Exists

B3 (family-mean threshold) is preserved as a Regime A comparator in the journal paper. It must not be silently dropped. Reproduction confirms stored results are valid.

## Scope

- Recompute B3 thresholds from verified Regime A scores using `DEVICE_FAMILY_MAP`
- Compare against stored B3 `metrics.json`
- Produce B3 row for journal table

## Out of Scope

- B3 on Regime B, C, or D (B3 is Regime A only)
- Modifying B3 logic

## Human Intervention Required

Status: `NO`

## Dependencies

- Previous ticket: T04 (reuse verdict SAFE)
- Required files: Verified Regime A score Parquets, stored B3 metrics
- Required decisions: None
- Required artifacts: Cell verdict table

## Previous-Ticket Check

Before starting: confirm T01–T04 are `DONE` or correctly blocked.

## Existing Code to Inspect First

- `src/datp/baselines/main/b3.py` — B3 implementation
- `src/datp/baselines/common/thresholds.py` — family arithmetic mean
- `src/datp/data/datasets/nbaiot/spec.py` — `DEVICE_FAMILY_MAP`

## Existing Tests to Inspect First

- `tests/unit/baselines/main/` — B3 tests

## Files Likely Touched

- `src/datp/` (verification script, likely minimal)
- `outputs/analysis/` (B3 reproduction artifact)

## Implementation Requirements

1. Load verified Regime A scores
2. Apply B3 logic: arithmetic mean threshold within each device family
3. Evaluate metrics
4. Compare against stored `outputs/results/a/b3/seed_*/metrics.json`
5. Produce B3 table row artifact

## Refactoring Requirements

- Reuse existing B3 logic entirely — no new code for threshold computation

## Schema, Enum, Constant, and Config Checks

- `DEVICE_FAMILY_MAP` in `nbaiot/spec.py` is the canonical source
- B3 uses `ThresholdAggregationMethod.ELIGIBLE_FAMILY_ARITHMETIC_MEAN`

## Test Requirements

- Unit test: B3 reproduction matches stored reference within tolerance

## Commands to Run

- `make test-unit` (targeted tests)

## Full-Suite Trigger Condition

Not breaking — no full suite needed.

## Acceptance Criteria

1. B3 reproduces within tolerance for all 5 seeds
2. B3 table row produced for journal

## Stop Conditions

- Stop if B3 does not reproduce (investigate b3.py or stored metrics)

## Implementation Notes

This should be straightforward given T03 already verifies B3 metrics. This ticket specifically produces the journal-facing artifact and confirms B3 is preserved.
