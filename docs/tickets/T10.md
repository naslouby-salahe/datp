# T10 — B4 Feature Ablation

## Status

`NOT_STARTED`

## Phase

Phase 2 — Stored-score analyses (Group GB)

## Source Plan References

- `docs/journal/CODING_PLAN.md` §4 (GB-06)
- `docs/journal/EXPERIMENT_PLAN.md` §4 (Gate 1)

## Goal

Evaluate B4 fingerprint subsets and full 4-feature fingerprint `[mean, std, skew, p95]`. Produce an ablation table and cluster contingency figure. Verify full 4-feature version reproduces canonical B4.

## Why This Exists

Demonstrates that B4 fingerprints are meaningful (not arbitrary) and identifies which features contribute to cluster quality.

## Scope

- Compute B4 with subsets: each single feature, pairs, triples, full 4-feature
- Evaluate cluster quality (contingency with device family for Regime A)
- Compare CV(FPR) across subsets
- Verify full 4-feature reproduces canonical B4 results

## Out of Scope

- Changing canonical B4 K=3
- Adding new fingerprint features
- Regime B/C B4 ablation (unless trivial to add)

## Human Intervention Required

Status: `NO`

## Dependencies

- Previous ticket: T04 (reuse verdict SAFE)
- Required files: Verified Regime A score Parquets
- Required decisions: None
- Required artifacts: Cell verdict table

## Previous-Ticket Check

Before starting: confirm T01–T04 are `DONE` or correctly blocked.

## Existing Code to Inspect First

- `src/datp/baselines/main/b4.py` — B4 fingerprint computation and clustering
- `src/datp/baselines/common/thresholds.py` — cluster threshold

## Existing Tests to Inspect First

- `tests/unit/baselines/main/` — B4 tests if they exist

## Files Likely Touched

- `src/datp/` (B4 ablation analysis module)
- `tests/unit/` (new tests)
- `outputs/analysis/` (output artifacts)

## Implementation Requirements

1. Extract fingerprint computation from `b4.py` to allow subset selection
2. Run B4 clustering with each feature subset
3. Evaluate contingency: cluster assignments vs device family labels
4. Evaluate CV(FPR) per subset
5. Verify full 4-feature reproduces B4 reference
6. Output: ablation table + contingency figure

## Refactoring Requirements

- If `b4.py::compute_fingerprints()` is tightly coupled, extract a parameterized version
- Do not modify the canonical B4 path — ablation is analysis-only

## Schema, Enum, Constant, and Config Checks

- Fingerprint feature names should be constants (likely already in b4.py)
- K=3 canonical for Regime A (from config)

## Test Requirements

- Unit test: full 4-feature fingerprint matches canonical B4
- Unit test: single-feature fingerprint produces valid clusters
- Unit test: contingency computation correct for known assignments

## Commands to Run

- `make test-unit` (targeted tests)

## Full-Suite Trigger Condition

Not breaking — no full suite needed.

## Acceptance Criteria

1. Full 4-feature ablation reproduces B4 reference
2. All subsets evaluated
3. Contingency table/figure produced
4. Ablation table shows relative contribution of each feature

## Stop Conditions

- Stop if full 4-feature does not reproduce B4 (investigate b4.py)

## Implementation Notes

`b4.py` already has `compute_fingerprints()` that takes explicit q parameter. Check if feature selection can be parameterized without modifying the canonical path.
