# T25 — Temporal Recalibration Probe

## Status

`DONE`

## Phase

Phase 4 — Experiment execution (Gate 4 — Temporal)

## Source Plan References

- `docs/journal/CODING_PLAN.md` §8 (GF-01 to GF-05)
- `docs/journal/PRE_CODING_PLAN.md` §5.11 (Temporal Validity)
- `docs/journal/PRE_CODING_PLAN.md` §6.5 (Temporal Lock)
- `docs/journal/EXPERIMENT_PLAN.md` §8 (Gate 4)

## Goal

If temporal feasibility is confirmed (from Edge-IIoTset or B-b data with timestamps): create chronological split, train on early window, evaluate frozen thresholds on late window, perform one-shot benign-only recalibration, and report recovery ratio.

## Why This Exists

Tests whether per-client thresholds degrade over time and whether one-shot recalibration can recover performance. This is a temporal probe, not a concept-drift solution.

## Scope

- Chronological 70/30 split per client
- Train AE on early benign data
- Calibrate thresholds on early benign calibration data
- Evaluate frozen thresholds on late data
- One-shot benign-only recalibration at boundary
- Report recovery ratio and FPR stability
- Allowed outcomes: helps, neutral, hurts/insufficient, infeasible

## Out of Scope

- Streaming drift detection
- Continuous recalibration
- Using attack labels in calibration/recalibration

## Human Intervention Required

Status: `RESOLVED` (H01 closed 2026-05-24; H02 closed 2026-05-24)

- Edge-IIoTset: `frame.time` timestamps confirmed populated in raw CSVs → temporal probe FEASIBLE on Regime D.
- CICIoT2023: raw schema audit (H02) confirmed **no timestamp column** of any kind → temporal probe INFEASIBLE on Regime B-b.
- Net effect: T25 proceeds using Edge-IIoTset data only. Any temporal claim on CICIoT2023 must be explicitly suppressed in the report.

## Dependencies

- Previous ticket: T21 (Edge-IIoTset preprocessing) — this is the live temporal path.
- Required files: Edge-IIoTset preprocessed data with `frame.time` preserved and chronological ordering.
- Required decisions: `TEMPORAL_FEASIBLE` outcome assigned on Edge-IIoTset; explicit `TEMPORAL_REJECTED_NO_TIMESTAMPS` suppression for CICIoT2023.
- Required artifacts: Preprocessed Edge-IIoTset splits with chronological ordering.

## Previous-Ticket Check

Before starting: confirm T21 is `DONE` with `TEMPORAL_FEASIBLE` assigned on Edge-IIoTset.

## Existing Code to Inspect First

- `src/datp/training/fl/runner.py` — FL training
- `src/datp/baselines/common/thresholds.py` — threshold computation
- `src/datp/data/splits.py` — split definitions

## Existing Tests to Inspect First

- `tests/unit/training/fl/`
- `tests/unit/baselines/main/test_thresholds.py`

## Files Likely Touched

- `src/datp/` (temporal split and recalibration module)
- `tests/unit/` (new tests)
- `outputs/` (temporal artifacts)

## Implementation Requirements

1. Temporal feasibility gate: timestamps exist, parse, are not constant, adequate coverage
2. Chronological split: 70% early / 30% late per client
3. Early-window training: AE trained on early benign data only
4. Early-window calibration: B1/B2/B4 thresholds from early benign cal
5. Frozen evaluation: apply early thresholds to late data
6. One-shot recalibration: recompute thresholds from later benign window (NO attack labels)
7. Recovery ratio: improvement in CV(FPR) after recalibration vs frozen
8. Report outcome per locked interpretations

## Refactoring Requirements

- Reuse existing FL training with temporal data subset
- Reuse existing threshold/metric code

## Schema, Enum, Constant, and Config Checks

- Temporal feasibility outcomes as enum (from PRE_CODING_PLAN §6.5)
- No attack labels in any calibration/recalibration path (guard check)

## Test Requirements

- Unit test: no attack labels in recalibration (guard)
- Unit test: recovery ratio computation correct
- Unit test: temporal feasibility check logic
- Integration test: temporal pipeline on small data

## Commands to Run

- None until unblocked

## Full-Suite Trigger Condition

New training variant — run relevant tests after stabilization.

## Acceptance Criteria

1. Temporal feasibility assessed
2. If feasible: complete temporal pipeline produces results
3. If infeasible: explicit suppression with reason
4. No attack labels in calibration/recalibration
5. Outcome classified: helps/neutral/hurts/infeasible

## Stop Conditions

- Blocked until T21 produces Edge-IIoTset preprocessed data with chronological ordering
- CICIoT2023 path is permanently suppressed under `TEMPORAL_REJECTED_NO_TIMESTAMPS`
- Stop and suppress if Edge-IIoTset attacks are temporally clustered making comparison invalid

## Implementation Notes

If temporal feasibility is rejected, produce suppression note with canonical wording from PRE_CODING_PLAN §5.11. No pseudo-temporal split is substituted.
