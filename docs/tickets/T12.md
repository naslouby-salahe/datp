# T12 — Threshold-Shift vs ΔFPR/ΔTPR

## Status

`DONE`

## Resolution Record (DONE on 2026-05-24)

- New module `src/datp/analyses/mechanism/threshold_shift.py`:
  - `run_threshold_shift(base_dir, *, config=None, write_outputs=False) → ThresholdShiftResult`
  - τ_B2 − τ_B1 per client, ΔFPR, ΔTPR
  - All clients included — no filtering
  - `ThresholdShiftRow` / `ThresholdShiftResult` pydantic schemas (extra='forbid', frozen=True)
- Writes `threshold_shift_table.csv` + `threshold_shift_fpr.png` + `threshold_shift_tpr.png`
- No retraining — reads stored scores only
- Tests: `tests/unit/analyses/mechanism/test_threshold_shift.py` (5 cases)
- ruff: 0 errors. pyright: 0 errors.

## Phase

Phase 2 — Stored-score analyses (Group GB)

## Source Plan References

- `docs/journal/CODING_PLAN.md` §4 (GB-08)
- `docs/journal/EXPERIMENT_PLAN.md` §4 (Gate 1)

## Goal

Explain per-client effect of threshold movement from B1 to B2. Produce scatter plot of threshold shift vs ΔFPR and ΔTPR per client.

## Why This Exists

Shows mechanistically how per-client threshold adjustment changes FPR and TPR for each device. All Regime A devices must be included.

## Scope

- Compute per-client threshold shift: τ_B2_i − τ_B1
- Compute per-client ΔFPR = FPR(B1) − FPR(B2)
- Compute per-client ΔTPR = TPR(B2) − TPR(B1)
- Scatter plots: threshold-shift vs ΔFPR, threshold-shift vs ΔTPR
- Per-client table with all values

## Out of Scope

- Causal interpretation
- Filtering any clients from the analysis

## Human Intervention Required

Status: `NO`

## Dependencies

- Previous ticket: T04 (reuse verdict SAFE)
- Required files: Verified Regime A score Parquets + stored thresholds/metrics
- Required decisions: None
- Required artifacts: Cell verdict table

## Previous-Ticket Check

Before starting: confirm T01–T04 are `DONE` or correctly blocked.

## Existing Code to Inspect First

- `src/datp/baselines/common/thresholds.py` — threshold values
- `src/datp/evaluation/metrics.py` — per-client FPR/TPR
- `src/datp/evaluation/confusion.py` — confusion matrix per client

## Existing Tests to Inspect First

- `tests/unit/evaluation/` — metric tests

## Files Likely Touched

- `src/datp/` (analysis module)
- `tests/unit/` (new tests)
- `outputs/analysis/` (output artifacts)

## Implementation Requirements

1. Load per-client B1 and B2 thresholds (or recompute from scores)
2. Compute threshold shift per client
3. Load or compute per-client FPR/TPR under both B1 and B2
4. Compute deltas
5. Scatter figures + per-client table
6. All 9 Regime A devices included — no filtering

## Refactoring Requirements

- Reuse existing threshold and metric computation

## Schema, Enum, Constant, and Config Checks

- No new enums or constants needed

## Test Requirements

- Unit test: threshold shift computation correct for known values
- Unit test: all clients included in output

## Commands to Run

- `make test-unit` (targeted tests)

## Full-Suite Trigger Condition

Not breaking — no full suite needed.

## Acceptance Criteria

1. All 9 Regime A devices included
2. Scatter plots produced
3. Per-client table produced
4. No client filtered out

## Stop Conditions

- Stop if per-client metrics are unavailable (return to T03)

## Implementation Notes

Simple mechanistic analysis. The value is in completeness (all devices shown) and clarity.
