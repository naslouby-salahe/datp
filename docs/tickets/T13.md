# T13 — Alert-Burden Table

## Status

`DONE (SUPPRESSED)`

## Resolution Record (DONE on 2026-05-24)

- **Suppressed**: N-BaIoT raw data contains only 115 statistical features — NO timestamps, NO flow rates, NO packet counts, NO per-device-day rate data.
- Per T13 stop condition: never invent rates → suppression note is the correct output.
- New module `src/datp/analyses/alert_burden.py`:
  - `run_alert_burden(base_dir, *, write_outputs=False) → AlertBurdenSuppression`
  - `AlertBurdenSuppression` pydantic schema with reason + recommendation
- Writes `alert_burden_suppression.json`
- Tests: `tests/unit/analyses/test_alert_burden.py` (4 cases)
- ruff: 0 errors. pyright: 0 errors.

## Phase

Phase 2 — Stored-score analyses (Group GB)

## Source Plan References

- `docs/journal/CODING_PLAN.md` §4 (GB-09)
- `docs/journal/EXPERIMENT_PLAN.md` §4 (Gate 1)

## Goal

Translate FPR to alerts/device/day using real timestamped flow/packet rates from the dataset or a cited dataset-specific operational rate. Omit entirely if neither is available.

## Why This Exists

Provides operational context for FPR differences. Reviewers appreciate concrete alert numbers. However, the plan strictly requires real-derived or cited rates — no invented numbers.

## Scope

- Determine if N-BaIoT has real timestamps or a citable flow rate
- If available: compute alerts/device/day = FPR × flows_per_day
- Compare B1 vs B2 alert burden per device
- If NOT available: produce explicit suppression note and omit

## Out of Scope

- Inventing flow rates
- Using assumed operational rates without citation
- Alert-fatigue modeling

## Human Intervention Required

Status: `NO` (but may result in suppression if no rate source exists)

## Dependencies

- Previous ticket: T04 (reuse verdict SAFE)
- Required files: N-BaIoT dataset metadata, any timestamp/rate information
- Required decisions: Whether a citable rate exists
- Required artifacts: Cell verdict table

## Previous-Ticket Check

Before starting: confirm T01–T04 are `DONE` or correctly blocked.

## Existing Code to Inspect First

- `src/datp/data/datasets/nbaiot/spec.py` — dataset spec
- N-BaIoT raw data metadata (if timestamps exist)

## Existing Tests to Inspect First

- None directly relevant

## Files Likely Touched

- `src/datp/` (analysis module, if rate is available)
- `outputs/analysis/` (alert table or suppression note)

## Implementation Requirements

1. Investigate N-BaIoT for timestamp fields or documented flow rates
2. If rate available: `alerts_per_day = FPR × rate × seconds_per_day`
3. If rate NOT available: write suppression note as output artifact
4. Never invent or assume rates

## Refactoring Requirements

- Minimal — this is analysis-only

## Schema, Enum, Constant, and Config Checks

- Flow rate (if found) should be a dataset-spec constant, not inline

## Test Requirements

- Unit test: alert computation correct for known FPR and rate
- Unit test: suppression note produced if rate is None

## Commands to Run

- None for suppression case
- `make test-unit` (targeted tests) if implemented

## Full-Suite Trigger Condition

Not breaking — no full suite needed.

## Acceptance Criteria

1. Either: alert-burden table with real-derived rates, OR explicit suppression note
2. No invented rates in any output

## Stop Conditions

- Stop and produce suppression note if no real rate source is found
- Never invent a rate

## Implementation Notes

N-BaIoT likely has timestamps in raw pcap/CSV format. Check `data/raw/N-BaIoT/` structure. If only feature vectors without timestamps exist in processed form, check if raw data has timing info. If not, suppress cleanly.
