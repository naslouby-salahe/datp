# T19 — Stress-Test Threshold Grid and Absorption Reporting

## Status

`NOT_STARTED`

## Phase

Phase 3 — Stress-test implementation (Group GE)

## Source Plan References

- `docs/journal/CODING_PLAN.md` §7 (GE-03, GE-04, GE-05)
- `docs/journal/EXPERIMENT_PLAN.md` §7 (Gate 3)

## Goal

Apply B1/B2/B4 thresholds to stress-test scores (FedProx and Ditto/FedRep-AE). Apply B-FedStatsBenign if needed. Produce absorption-category table using pre-specified rules.

## Why This Exists

Combines stress-test scores with threshold variants to answer: does the B1→B2 gain survive under different aggregation/personalization? Produces the final stress-test reporting artifacts.

## Scope

- Apply B1/B2/B4 to FedProx scores (per µ)
- Apply B1/B2/B4 to personalization scores
- Optionally apply B-FedStatsBenign to stress-test scores
- Compute absorption ratios
- Classify: strong retention / partial absorption / near-full absorption
- Produce stress-test × threshold table

## Out of Scope

- Retraining (uses scores from T17/T18)
- Post-hoc tuning of µ or absorption thresholds
- Promoting stress tests to core ladder

## Human Intervention Required

Status: `NO`

## Dependencies

- Previous ticket: T17 (FedProx scores), T18 (personalization scores)
- Required files: Stress-test score Parquets from T17/T18
- Required decisions: None (absorption rules locked)
- Required artifacts: FedProx and personalization scores

## Previous-Ticket Check

Before starting: confirm T17–T18 are `DONE` or correctly blocked.

## Existing Code to Inspect First

- `src/datp/baselines/common/thresholds.py` — threshold application
- `src/datp/evaluation/metrics.py` — metric computation
- T09 B-FedStatsBenign implementation

## Existing Tests to Inspect First

- `tests/unit/baselines/main/test_thresholds.py`
- `tests/unit/evaluation/`

## Files Likely Touched

- `src/datp/` (stress-test evaluation module)
- `tests/unit/` (new tests)
- `outputs/analysis/` (output artifacts)

## Implementation Requirements

1. Load stress-test scores (FedProx per µ, personalization)
2. Apply canonical threshold derivation for B1/B2/B4
3. Compute CV(FPR) and other metrics per (stress-test, threshold, µ)
4. Compute absorption ratio per lock formula
5. Classify using locked thresholds: ≥0.75, [0.25, 0.75), <0.25
6. Apply B-FedStatsBenign if reporting requires it (no attack labels)
7. Produce absorption-category table

## Refactoring Requirements

- Reuse canonical threshold and metric functions
- Same evaluation path as core baselines, just different scores

## Schema, Enum, Constant, and Config Checks

- Absorption thresholds from config
- Use existing `Baseline` enum for threshold identification
- Stress tests are NOT baselines

## Test Requirements

- Unit test: absorption ratio computation correct
- Unit test: classification at boundary values
- Unit test: canonical threshold reuse verified

## Commands to Run

- `make test-unit` (targeted tests)

## Full-Suite Trigger Condition

Not breaking — uses existing threshold/metric code on new scores.

## Acceptance Criteria

1. B1/B2/B4 applied to all stress-test scores
2. Absorption ratios computed per lock formula
3. Classifications assigned without post-hoc adjustment
4. Stress-test × threshold table produced
5. No stress test in core ladder

## Stop Conditions

- Stop if T17 or T18 scores are unavailable
- Stop if canonical thresholds produce errors on stress-test scores (investigate)

## Implementation Notes

This ticket is primarily evaluation — applying existing threshold/metric logic to new score arrays. The key output is the absorption-category table that determines journal claim wording per POST_EXPERIMENT_PLAN §2.7.
