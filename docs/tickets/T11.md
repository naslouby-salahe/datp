# T11 — JS Divergence vs DATP Benefit

## Status

`DONE`

## Resolution Record (DONE on 2026-05-24)

- New module `src/datp/analyses/js_divergence_benefit.py`:
  - `run_js_divergence(base_dir, *, config=None, write_outputs=False) → JSDivergenceResult`
  - Per-client JS(P_client ‖ P_pooled) using `js_divergence_n_bins` from config
  - ΔFPR = FPR(B1) − FPR(B2) per client
  - Spearman ρ via canonical `spearman_correlation()`
  - `JSClientRow` / `JSDivergenceResult` pydantic schemas (extra='forbid', frozen=True)
- Writes `js_divergence_table.csv` + `js_divergence_scatter.png`
- No retraining — reads stored scores only
- Tests: `tests/unit/analyses/test_js_divergence_benefit.py` (7 cases)
- ruff: 0 errors. pyright: 0 errors.

## Phase

Phase 2 — Stored-score analyses (Group GB)

## Source Plan References

- `docs/journal/CODING_PLAN.md` §4 (GB-07)
- `docs/journal/EXPERIMENT_PLAN.md` §4 (Gate 1)

## Goal

Relate Jensen-Shannon divergence between client score distributions to per-client B1−B2 FPR gain. Produce scatter plot and ρ/R² table.

## Why This Exists

Provides mechanistic support: if heterogeneity (measured by JS divergence) correlates with DATP benefit, the threshold personalization rationale is strengthened. This is non-causal/descriptive only.

## Scope

- Compute JS divergence between each client's calibration distribution and the global pool
- Compute per-client ΔFPR = FPR(B1) − FPR(B2)
- Scatter plot: JS divergence vs ΔFPR per client
- Compute Spearman ρ and R² (or rank correlation)
- Report for Regime A

## Out of Scope

- Causal claims
- Other divergence measures beyond JS
- Regime C/D (unless trivial addition)

## Human Intervention Required

Status: `NO`

## Dependencies

- Previous ticket: T04 (reuse verdict SAFE)
- Required files: Verified Regime A score Parquets
- Required decisions: None
- Required artifacts: Cell verdict table

## Previous-Ticket Check

Before starting: confirm T01–T04 are `DONE` or correctly blocked.

## Existing Code to Inspect First

- `src/datp/statistics/divergence.py` — existing JS divergence implementation
- `src/datp/statistics/spearman.py` — Spearman correlation
- `src/datp/evaluation/metrics.py` — per-client FPR

## Existing Tests to Inspect First

- `tests/unit/statistics/test_statistics.py`

## Files Likely Touched

- `src/datp/` (analysis module)
- `tests/unit/` (new tests)
- `outputs/analysis/` (output artifacts)

## Implementation Requirements

1. Compute JS divergence per client using existing `divergence.py`
2. Compute per-client FPR under B1 and B2 from existing metrics
3. ΔFPR = FPR_B1 - FPR_B2 per client
4. Scatter: JS vs ΔFPR with all Regime A devices
5. Spearman ρ using existing `spearman.py`
6. Output: scatter figure + ρ/R² table

## Refactoring Requirements

- Reuse existing `divergence.py` and `spearman.py` — do not reimplement
- Ensure JS divergence uses canonical n_bins from config

## Schema, Enum, Constant, and Config Checks

- `js_divergence_n_bins` already in config (from BASE_CONFIG)
- No new enums needed

## Test Requirements

- Unit test: known distributions → known JS divergence (sanity)
- Unit test: all clients included (no filtering)

## Commands to Run

- `make test-unit` (targeted tests)

## Full-Suite Trigger Condition

Not breaking — no full suite needed.

## Acceptance Criteria

1. Scatter plot includes all 9 Regime A devices
2. ρ and R² reported
3. Uses canonical JS divergence implementation
4. Non-causal framing in any output metadata

## Stop Conditions

- Stop if JS divergence implementation is missing or broken

## Implementation Notes

Weak correlation is reported honestly as "non-causal/limited." The result is descriptive regardless of direction.
